from django.urls import path
from . import views
from django.conf.urls import url

urlpatterns = [
    # url(r'^upload/$', views.ImageUploadView.as_view(), name='upload'),
    # url(r'^list/$', views.ImageListView.as_view(), name='list'),
]